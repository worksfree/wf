# LifeArt 홈페이지 — 10일차 QA 체크리스트

## 점검 완료
- [x] 전체 19개 페이지의 내부 링크(`href="/..."`) 경로 실제 파일 존재 확인
- [x] `href="#"` (미구현) 링크 전체에 `.wip` 클래스 + "개발 예정" 툴팁 적용 확인 — 누락 없음
- [x] JS 4개 파일(auth/config/forms/layout) + Worker 소스 구문 검사 통과
- [x] 배포 시 `worker/`, `supabase/` 폴더가 NAS 웹 루트에 노출되지 않도록 deploy.ps1 제외 처리
- [x] `.wrangler` 캐시(계정 정보 포함) git 추적 제외

## 실제 동작 확인된 흐름
- [x] 회원가입 → profiles 자동 생성 → 로그인 → 헤더 "로그인" → "마이페이지" 전환
- [x] 액자 주문 → Supabase orders 생성 → 토스 결제창 → Worker 승인 검증 → orders.status='paid' + payments 기록
- [x] 문의/견적/제휴 폼 → inquiries 테이블 INSERT (로그인 시 user_id 연결)
- [x] 관리자 페이지 — profiles.role='admin' RLS로 주문/문의 조회 및 상태 변경

## 알려진 제한사항 (실 운영 전 확인 필요)
- **토스페이먼츠 키**: 현재 토스 공용 테스트 샌드박스 키 사용 중. 실 결제 전 LifeArt 명의 라이브 키로 교체 필수(`assets/config.js`, Worker secret).
- **포토북**: 메뉴 구조상 "Coming Soon" — 이번 10일 스코프에 포함하지 않음(계약서 스코프와 일치).
- **오시는 길 주소 / 보도자료**: 실제 확정 정보를 받지 못해 "확인 중 / 준비 중"으로 표기. 정보 확정 시 반영 필요.
- **삽입식앨범 · VIP앨범**: 정찰가가 없어 상담형으로 처리(주문 대신 견적 문의 유도). 가격 정책이 나오면 액자와 동일한 주문 흐름으로 확장 가능.
- **브라우저 실사용 테스트**: 이 체크리스트는 코드/링크 정합성 점검이며, 실제 브라우저에서의 시각적 QA(모바일 레이아웃, 결제창 실제 클릭 등)는 별도로 필요.

## 배포 이력 (git 태그)
`lifeart-day2` ~ `lifeart-day9` — 각 태그는 해당 일자까지의 완성 상태를 가리킴.
단계적 배포 시 `git archive <tag>`로 해당 시점 스냅샷만 test-lifeart에 전송.
