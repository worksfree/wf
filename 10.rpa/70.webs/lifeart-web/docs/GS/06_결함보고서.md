# LifeArt 웹서비스 결함 보고서

| 항목 | 내용 |
|---|---|
| 문서 번호 | LA-QA-DEF-001 |
| 시험 회차 | 2026-07-19 GS 수준 자동화 시험 (04 문서) |
| 요약 | 발견 3건 → 수정 3건 → 재시험 전건 PASS. 별도로 성능 개선 1건(이미지 최적화) 수행 |

## DF-01 관리자 페이지 비로그인 콘솔 400 오류
- **심각도**: 중 (기능 영향 없음, 콘솔 오류·불필요 네트워크 요청)
- **증상**: 로컬 환경에서 /admin/ 접속 시 Supabase REST 400 오류 3건 발생 (A-02 FAIL)
- **원인**: 로컬 개발용 인증 게이트 우회 경로에서 세션 없이 RLS 보호 테이블 쿼리를 실행
- **수정**: [admin/index.html] initAdmin 에서 세션이 없으면 데이터 로드를 생략 (게이트 우회 미리보기는 UI만 표시)
- **재시험**: A-01/A-02 PASS. 운영 도메인 동작(게이트 차단)은 변경 없음

## DF-02 모바일 뷰포트 가로 스크롤
- **심각도**: 중 (모바일 사용성 저하)
- **증상**: 375px 뷰포트에서 전 페이지 45px 가로 오버플로 (B-01 FAIL)
- **원인**: 헤더 `.nav-actions { min-width:480px }` (데스크톱 버튼 폭 고정용)가 모바일 뷰포트보다 넓음
- **수정**: [assets/style.css] `@media (max-width:860px)` 에서 min-width 해제 + flex-wrap
- **재시험**: B-01~03 PASS

## DF-03 비로그인 시 회원가입/로그인 버튼 미표시 (전 회차 발견, 본 형상에 수정 포함)
- **심각도**: 상 (핵심 진입 동선 차단)
- **증상**: 비로그인 상태에서 헤더 우측 버튼(카탈로그·회원가입·로그인)이 표시되지 않음
- **원인**: `.nav-actions` 는 `auth-ready` 클래스 전까지 visibility:hidden 인데, auth.js 가 비로그인 분기에서 클래스를 붙이기 전에 return
- **수정**: [assets/auth.js] try/finally 로 어떤 경로든 auth-ready 부여 (세션 조회 실패 포함)
- **재시험**: A-09 PASS

## 개선 IMP-01 이미지 용량 최적화 (D-03 기준 충족)
- **내용**: 참조 이미지 중 500KB 초과 24건을 재압축·변환 (JPG 재압축 q80·최대폭 1600 / 사진성 PNG→JPG 변환 + 참조 치환)
- **효과**: 약 45MB → 약 3MB (예: wedding-real.jpg 3,399KB→46KB, corp-03.jpg 2,794KB→52KB, hero-vip 1,835KB→166KB)
- **검증**: 참조 무결성 372건 404 0건(A-03), D-03 PASS
