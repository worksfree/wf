name               = "lifeart-toss-verify"
main               = "index.js"
compatibility_date = "2025-01-01"

# 시크릿은 아래 명령으로 별도 설정 (여기에 직접 쓰지 말 것):
#   wrangler secret put TOSS_SECRET_KEY           --config wrangler.toml
#   wrangler secret put SUPABASE_URL              --config wrangler.toml
#   wrangler secret put SUPABASE_SERVICE_ROLE_KEY --config wrangler.toml
