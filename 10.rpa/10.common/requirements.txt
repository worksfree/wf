# Google Sheets 접근을 위한 gspread (간단하고 실용적)
gspread>=5.0.0
google-auth>=2.0.0

# 기존 Google Sheets API (대안으로 유지)
# google-api-python-client>=2.0.0
# google-auth-oauthlib>=1.0.0
# google-auth-httplib2>=0.2.0

# 데이터 처리 (선택사항)
pandas>=1.3.0

# 기본 라이브러리들은 Python 기본 내장이므로 별도 설치 불필요:
# - tkinter (GUI)
# - json (JSON 처리)
# - datetime (날짜/시간)
# - hashlib (해시 생성)
# - platform (시스템 정보)
# - subprocess (시스템 명령어)
# - pathlib (경로 처리)
# - os (운영체제 인터페이스)
# - sys (시스템 매개변수)
# - threading (멀티스레딩)
# - uuid (고유 ID 생성)